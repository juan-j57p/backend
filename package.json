{
  "name": "part3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../../.. && cd My_course/part2/phonebook && npm run build --prod && cp -r build ../../../curso-de-fullstack/sección2/part3/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "dev": "nodemon index.js",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "17.3.1",
    "express": "5.1.0",
    "mongoose": "9.1.2",
    "morgan": "1.10.0"
  },
  "devDependencies": {
    "nodemon": "3.1.10",
    "standard": "17.1.2"
  },
  "eslintConfig": {
    "extends": "./node_modules/standard/eslintrc.json"
  }
}
